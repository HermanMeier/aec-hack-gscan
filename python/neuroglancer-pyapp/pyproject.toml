[project]
name = "neuroglancer-pyapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "neuroglancer>=2.40.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "tensorstore>=0.1.68",
]
